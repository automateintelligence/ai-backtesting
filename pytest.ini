[pytest]
addopts = --cov=quant_scenario_engine --cov-report=term-missing --cov-fail-under=80
pythonpath = .

# Coverage thresholds per Constitution Principle XIX and FR-076-FR-077:
# - Line coverage ≥80% across qse/{optimizers,pricing,distributions,scorers}
# - Critical paths (Stage 0-4, MC scoring, composite scoring) require 100% branch coverage
# - Test suite runtime <2 minutes for fast feedback loops (FR-079)
